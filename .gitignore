# --- secrets ---
.env
.env.*
!.env.example

# --- backend (Python) ---
backend/__pycache__/
**/__pycache__/
*.pyc
backend/.venv/
backend/venv/

# Regeneratable runtime response cache (geocode/route). NOT the graph/building/
# tree data files below — those are committed so Render loads from disk instead
# of hitting Overpass at startup (which Render's network policy blocks).
backend/cache/

# --- frontend (Node) ---
frontend/node_modules/
frontend/dist/
